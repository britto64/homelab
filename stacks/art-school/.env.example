# Host port. Inside the container it always listens on 3000.
ARTSCHOOL_PORT=3000

# Course library. Mounted :ro — the app never writes here.
COURSES_HOST_PATH=/path/to/courses

# SQLite database and generated thumbnails. This is what backups must cover.
DATA_HOST_PATH=/path/to/artschool/data
