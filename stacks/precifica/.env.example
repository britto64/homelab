# Image tag, pinned. Never `latest`: this is the line that answers "what is
# running?" and the one a rollback edits. Published by scripts/deploy.sh from
# the 6.precifica repo, version taken from package.json.
PRECIFICA_VERSION=1.0.0